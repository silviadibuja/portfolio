---
import ProfileImage from "../../public/img/ilustracion_silvia.png";
import Waves from "./Waves.astro";
---

<style>
    .about-section {
        position: absolute;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        z-index: 20;

        .about-img {
            width: 30%;

            img {
                width: 100%;
                height: auto;
                display: block;
            }
        }

        .about-container {
            display: flex;
            flex-direction: column;
            gap: 1rem;

            h2, h3, p {
                color: var(--color-text);
            }

            .description {
                display: flex;
                flex-direction: column;
                align-items: baseline;
                row-gap: 1.5rem;
            }
        }
    }

    @media screen and (min-width: 48rem) {
        .about-section {
            flex-direction: row;

            .about-container {
                width: 40%;
            }
        }

        .projects-container {
            flex-direction: row;
        }
    }

    .about-img {
        display: block;
        height: auto;
        object-fit: cover;
        justify-self: center;
    }

    @media screen and (min-width: 64rem) {
        .about-img {
            margin-right: 5rem;
        }
    }

    .slide {
        opacity: 0;
        transition: opacity 0.6s ease;
        position: absolute;
        top: 0;
        left: 0;
    }

    .slide.active {
        opacity: 1;
        position: relative;
    }

    .dots {
        position: absolute;
        right: 1rem;
        top: 50%;
        transform: translateY(-50%);
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .dot {
        width: 10px;
        height: 10px;
        background-color: red;
        border-radius: 50%;
        transition: background-color 0.3s;
    }

    .dot.active {
        background-color: green;
    }
</style>

<section class="section s3">
    <Waves color="#eff3fa" mirror={true} withBackground={true} />

    <div class="about-section">
        <div class="about-img">
            <picture>
                <img src={ProfileImage.src} alt="Foto de perfil" srcset="" />
            </picture>
        </div>
        <div class="about-container">
            <div class="about-title">
                <h2>Soy Silvia Romero</h2>
            </div>
            <div class="description">
                <h3>
                    Ilustradora y escenógrafa con alma curiosa y manos
                    inquietas. 
                </h3>
                <p>
                    Aquí te cuento 5 cosas que no sabías (pero deberías) sobre mí.
                </p>
            </div>
        </div>
    </div>
</section>
