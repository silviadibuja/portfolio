---
import "@/assets/css/reset.css";
import "@/assets/css/styles.css";
import "@/assets/css/typography.css";
import ArrowBack from "@/components/ArrowBack.astro";
import ImageIA from "../../public/img/corto_web.jpg";
---

<style>
    /* PAGE IA */
    .section-ia {
        width: 100vw;
        height: 100dvh;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        padding: 2rem;
        max-width: 1200px;
        margin: 0 auto;
        background-color: var(--color-white-primary);
    }

    .section-ia-container {
        flex: 1;
        display: flex;
        flex-direction: column;
        gap: 2rem;
        justify-content: center;
    }

    picture {
        align-self: center;
        width: 100%;
        max-width: 600px;
    }

    picture img {
        width: 100%;
        height: 50vh;
        object-fit: cover;
        display: block;
        border-radius: 8px;
    }

    .content {
        text-align: center;
        padding: 0 1rem;
    }

    .content p {
        margin-bottom: 1rem;
        line-height: 1.4;
        font-size: 2.5rem;
    }

    #openVideo {
        margin-top: 1rem;
        padding: 0.75rem 4.5rem;
        background-color: var(--color-primary);
        color: var(--color-text);
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 3rem;
    }

    #openVideo:hover {
        background-color: var(--color-text);
        color: var(--color-white-primary);
    }

    .modal {
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100dvh;
        background: rgba(0, 0, 0, 0.8);
        display: flex;
        justify-content: center;
        align-items: center;
        z-index: 9999;
    }

    .modal-content {
        position: relative;
        width: 90%;
        max-width: 800px;
        aspect-ratio: 16 / 9;
    }

    .modal-content iframe {
        width: 100%;
        height: 100%;
        border: none;
        border-radius: 8px;
    }

    .close-btn {
        position: absolute;
        top: -1.5rem;
        right: -1.5rem;
        background: transparent;
        color: white;
        border: none;
        font-size: 2.5rem;
        font-weight: bold;
        cursor: pointer;
        line-height: 1;
        z-index: 10000;
        transition: transform 0.2s ease;
    }

    .close-btn:hover {
        transform: scale(1.2);
        color: var(--color-primary); /* O el color que prefieras */
    }

    .hidden {
        display: none;
    }

    @media (min-width: 64rem) {
        .section-ia-container {
            flex-direction: row;
            align-items: center;
            gap: 4rem;
        }

        picture {
            flex: 1;
            justify-items: center;
        }

        picture img {
            height: auto;
            max-height: 80vh;
            width: unset;
        }

        .content {
            flex: 1;
            text-align: left;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 0;
        }

        .content p {
            font-size: 1.5rem;
        }

        .content button {
            align-self: flex-start;
            font-size: 2rem;
            padding: 0.75rem 2.5rem;
        }
    }
</style>

<section class="section-ia">
    <ArrowBack />
    <div class="section-ia-container">
        <picture>
            <img src={ImageIA.src} alt="Cartel ilustracion" />
        </picture>
        <div class="content">
            <p>
                Un cortometraje ilustrado sobre la magia de la imaginación.
                Daniel, un niño de ocho años, transforma su estancia en el
                hospital en una gran aventura gracias a su cratividad. Una
                historia emotiva sobre resiliencia, fantasía y amistad.
            </p>
            <p>
                Proyecto final del curso de ilustración, realizado con el apoyo
                de herramientas de inteligencia artificial y técnicas digitales.
            </p>
            <div id="videoModal" class="modal hidden">
                <div class="modal-content">
                    <iframe
                        src="https://player.vimeo.com/video/1099899053?badge=0&autopause=0&player_id=0&app_id=58479"
                        frameborder="0"
                        allow="autoplay; fullscreen; picture-in-picture"
                        title="EL CLUB DE LOS INCREÍBLES"
                        allowfullscreen></iframe>
                    <button class="close-btn" aria-label="Cerrar modal">✕</button
                    >
                </div>
            </div>
            <button id="openVideo">Ver corto</button>
        </div>
    </div>
</section>

<script is:inline>
    document.querySelector("#openVideo")?.addEventListener("click", () => {
        document.querySelector("#videoModal")?.classList.remove("hidden");
    });

    document.querySelector(".close-btn")?.addEventListener("click", () => {
        const modal = document.querySelector("#videoModal");
        modal?.classList.add("hidden");
        const iframe = modal.querySelector("iframe");
        iframe.src = iframe.src; // Reset video
    });
</script>
<script>
    console.log("Viewport width:", window.innerWidth);
</script>
